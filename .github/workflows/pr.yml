name: pr

on:
  pull_request:
    branches: [main]

jobs:
  todo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO: Story 1.13 — author the real PR pipeline (lint, typecheck, vitest with 100% coverage, Playwright smoke + axe-core)."
